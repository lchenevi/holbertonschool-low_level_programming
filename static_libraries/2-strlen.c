#include "main.h"

/**
 *_strlen - calculate the length of a string
 *@s: checke
 *Return: return the length o a string
 */
int _strlen(char *s)
{
	int x = 0;

	while (x[s] != '\0')
		x++;
	return (x);
}
